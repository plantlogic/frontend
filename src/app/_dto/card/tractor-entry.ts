export class TractorEntry {
  // fertilizer	string
  fertilizer: string;
  // gallons	number($float)
  gallons: number;
  // workDate	string($date-time)
  workDate: number = Date.now();
  // workDone	string
  workDone: string;
  // operator string
  operator: string;
}
