export class User {
  email: string;
  username: string;
  realName: string;
  passwordUpdated: string;
  permissions: string[];
}

export const fields = ['Username', 'Name', 'Email', 'Password Last Updated', 'Permissions'];
